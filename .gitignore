# Carousel images are committed to the repo so they are accessible at
# zentraai.in/blog/carousels/<slug>/slide-N.jpg and shown inline on the post page.
# GitHub Actions also uploads them as artifacts as a backup download.
